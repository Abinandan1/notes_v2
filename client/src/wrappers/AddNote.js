import styled from "styled-components";
const Wrapper = styled.div`
  .btn {
    font-weight: bold;
    letter-spacing: 3px;
  }
  .form-row {
    overflow: hidden;
  }
  .form-input,
  .form-textarea {
    overflow: hidden;
  }
`;
export default Wrapper;
